/*class Heart extends Phaser.GameObjects.Image {
    constructor(scene, x, y, texture) {
        super(scene, x, y, texture);

        // Set the scale or any other properties you want
        this.setScale(0.05, 0.05);

        let heartX = game.config.width - borderUISize - (i * 30);
        let heartY = borderUISize;


        // Add this heart to the scene
        scene.add.existing(this);
    }
}
*/